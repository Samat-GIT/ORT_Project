@model IEnumerable<ORT_Project.Models.District_town_>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.District_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Password_district)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Region1.Region_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.District_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Password_district)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Region1.Region_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_District }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_District }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_District })
        </td>
    </tr>
}

</table>
