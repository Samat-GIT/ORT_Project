@model IEnumerable<ORT_Project.Models.Access_level>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Access_level1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Access_level1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_access_level }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_access_level }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_access_level })
        </td>
    </tr>
}

</table>
