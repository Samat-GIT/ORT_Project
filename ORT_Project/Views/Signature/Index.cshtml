@model IEnumerable<ORT_Project.Models.SignaturesTable>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Add")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FK_Graduant)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Photo3x4)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PhotoSignature)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FK_Graduant)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Photo3x4)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhotoSignature)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
