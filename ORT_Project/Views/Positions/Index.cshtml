@model IEnumerable<ORT_Project.Models.Position>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Position_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Position_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Position }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Position }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Position })
        </td>
    </tr>
}

</table>
